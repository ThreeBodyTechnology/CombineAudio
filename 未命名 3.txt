
首先是输入一个文件夹X,这个文件夹必须只包含音频文件,但可以有子目录,程序会记录
这个文件夹中所有的子目录以及子目录中的音频文件,同时目录结构依然被保存.

然后要再输入一个目录,程序会在这个目录下创建一个名叫X.lineup的文件夹,这个文件夹里包含一个
X.xml文件和数个wav大文件.

想要解析文件夹,将X.lineup文件夹输入,再输入一个新的文件夹,会在这个文件夹中创建一个与原先目录结构完全一致的文件夹,同时包含所有音频文件.